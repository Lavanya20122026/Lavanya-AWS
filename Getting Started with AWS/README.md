What was done ?
    Created AWS account using root user email
    Set up secure password (as per AWS requirements)
    Selected Free Tier plan for safe learning
    Completed:
    Email verification
    Phone verification
    Basic support plan selection
    Logged into AWS Management Console

Key concepts understood :
    AWS is a global cloud platform
    Services are divided into:
    Global services (e.g., IAM)
    Regional services (e.g., EC2)
    Each resource is tied to a region
    Region selection impacts:
        Latency
        Service availability
        Cost

Console familiarity
    Navigated AWS Console dashboard
    Understood:
    Region selector (top right)
    Services menu (by category / alphabetical)
    Search bar for quick access
    Observed:
        Billing info
        Account health
        Recently used services

Imp Notes :
    Always select region closest to users (for performance)
    Stick to one region during learning
    Not all services are available in every region
    AWS console layout may change, but core concepts remain same
    Forms the base for all AWS services
    Helps avoid confusion while creating resources
    Ensures correct region usage during deployments
